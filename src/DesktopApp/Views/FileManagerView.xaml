<UserControl x:Class="SuperPanel.DesktopApp.Views.FileManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="File Manager" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Path Navigation -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Path:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding CurrentPath, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Height="30"/>
            <Button Grid.Column="2" Content="Go" Width="50" Height="30" Margin="10,0,0,0"
                    Command="{Binding LoadDirectoryCommand}"/>
        </Grid>
        
        <!-- File List -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Files}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Background="Transparent">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Type" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsDirectory, Converter={StaticResource FileTypeIconConverter}}" 
                                       FontSize="16" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="300"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeBytes, Converter={StaticResource FileSizeConverter}}" Width="100"/>
                <DataGridTextColumn Header="Modified" Binding="{Binding LastModified, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Permissions" Binding="{Binding Permissions}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
    
    <UserControl.Resources>
        <local:FileTypeIconConverter x:Key="FileTypeIconConverter"/>
        <local:FileSizeConverter x:Key="FileSizeConverter"/>
    </UserControl.Resources>
</UserControl>